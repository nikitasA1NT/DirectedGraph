#pragma once
#include <vector>

// Sort vector using bubblesort
//void SortVect(std::vector<int>& vect);

// Sort vector using bubblesort and remove duplicate elements
void RemoveRepets(std::vector<int>& vect);

// Binary search
bool Search(std::vector<int> vect, int key);
